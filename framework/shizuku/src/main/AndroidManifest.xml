<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Shizuku's manager is not discoverable through an intent filter we can name up
      front: stealth mode renames the package, so the only reliable way to see it and
      deliver an explicit broadcast to it is broad package visibility. This app is
      sideloaded, not distributed through Play.
    -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <queries>
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <application>
        <!--
          Shizuku pushes its binder into this provider; the app never looks Shizuku up.
          The authority is this app's own package, and INTERACT_ACROSS_USERS_FULL is a
          signature permission no ordinary app can hold, so only the Shizuku server —
          running as shell or root — can call in.
        -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>
</manifest>
